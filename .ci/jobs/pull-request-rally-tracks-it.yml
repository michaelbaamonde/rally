---

- job:
    name: "elastic+rally+pull-request+rally-tracks-it"
    display-name: "elastic / rally # rally-tracks-it"
    description: "rally-tracks integration tests using rally PR"
    scm:
      - git:
          refspec: "+refs/pull/*:refs/remotes/origin/pr/* +refs/heads/*:refs/remotes/origin/*"
          branches:
            - "${ghprbActualCommit}"
    triggers:
      - github-pull-request:
          org-list:
            - elastic
          allow-whitelist-orgs-as-admins: true
          trigger-phrase: '.*run\W+rally/rally-tracks-it.*'
          github-hooks: true
          status-context: "rally/rally-tracks-it"
          cancel-builds-on-update: true
          black-list-labels:
            - '>test-mute'
    builders:
      - shell: |
          #!/usr/local/bin/runbld
          set -o errexit
          bash .ci/build.sh rally-tracks-it
